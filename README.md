# ccdc-rk-demo
## rootkit demo for ccdc practice
copy the .service into /etc/systemd/system/ and enable it
set the .service file owner to root
run/compile the kernel module in a sneaky location
move the .ko file and the https-spawn.sh
run the spawn-setup script as root
enjoy!
