# ccdc-rk-demo

## rootkit demo for ccdc practice

copy the .service into /etc/systemd/system/ and enable it - set the .service file owner to root

make /etc/https-handler

run/compile the kernel module in /etc/https-handler

run the spawn-setup script as root

enjoy!
